@model MainPageVM
@{
    ViewData["Title"] = "Index";
}

<div class="col-lg-10 offset-1">
    <div class="block">
        <div class="title text-center">
            <strong class="d-block">Section Name</strong> <span class="d-block">@Model.SectionService.SectionName</span>
            <a asp-area="Admin" asp-controller="ServiceItem" asp-action="SectionNameUpdate" asp-route-id="@Model.SectionService.Id" class="btn btn-primary btn-block text-white mt-3 btn-sm"><i class="fas fa-pencil-alt"></i> </a></td>
            <hr />
            <strong class="d-block">Testimonials Items</strong>
        </div>
        <div class="table-responsive my-5">
            <table class="table table-striped table-hover">
                <thead>
                    <tr class="text-center">
                        <th>Service Icon</th>
                        <th>Service Name</th>
                        <th>Details</th>
                        <th>Edit</th>
                        <th>Delete</th>
                    </tr>
                </thead>
                <tbody class="text-center">
                    @foreach (var item in Model.ServiveServiceItem)
                    {
                        <tr>
                            <td><i class="@item.ServiceIcon"></i></td>
                            <td>@item.ServiceName</td>
                            <td><a asp-area="Admin" asp-controller="ServiceItem" asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary btn-block text-white btn-sm"><i class="fas fa-info-circle"></i> </a></td>
                            <td><a asp-area="Admin" asp-controller="ServiceItem" asp-action="Update" asp-route-id="@item.Id" class="btn btn-primary btn-block text-white btn-sm"><i class="fas fa-pencil-alt"></i> </a></td>
                            <td><a asp-area="Admin" asp-controller="ServiceItem" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-block text-white btn-sm"><i class="fas fa-trash-alt"></i> </a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <hr />
        <a asp-area="Admin" asp-controller="ServiceItem" asp-action="Create" class="btn btn-success btn-block text-white">Create</a>
    </div>
</div>

